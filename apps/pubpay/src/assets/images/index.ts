// Import only the images we actually use using require to avoid TypeScript issues
const genericUserIcon = require('./generic-user-icon.svg');
const pubpayImg = require('./pubpay.jpg');

// Export the images
export { genericUserIcon, pubpayImg };
